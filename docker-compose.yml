version: "3"

services:
  mysql:
    image: mysql:8.0
    container_name: project-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: building_a_material_management_system_tdc
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    build: ./backend
    container_name: project-backend
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www/html
    depends_on:
      - mysql

  frontend:
    build: ./client
    container_name: project-client
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    environment:
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - backend

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: project-phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: 1234
      UPLOAD_LIMIT: 128M
    depends_on:
      - mysql

  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: project-meilisearch
    restart: unless-stopped
    environment:
      - MEILI_MASTER_KEY=mysecret
    ports:
      - "7700:7700"

volumes:
  mysql_data: